services:
  - type: web
    name: credit-expansion-engine-api
    env: python
    plan: free
    buildCommand: "pip install -r requirements-api.txt"
    startCommand: "uvicorn api:app --host 0.0.0.0 --port $PORT --app-dir src"
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
